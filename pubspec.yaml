name: food_safety_app
description: AI-Powered Food Safety Monitoring App

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.5
  dio: ^5.4.3
  image_picker: ^1.1.3
  cached_network_image: ^3.3.0
  intl: ^0.19.0
  shared_preferences: ^2.2.4
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true